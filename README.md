• Implemented a synchronous UART communication system to enhance debugging capabilities for a traffic signal controller.
• Designed transmitter and receiver units using storage and shift registers, developed a baud rate generator, utilized FSM for efficient data han‑
dling, and integrated the UART module into a traffic light control system.
• Transmitter converts parallel data into a serial stream, receiver processes incoming serial data, baud rate generator ensures synchronization,
and debug messages are generated for real‑time monitoring.
• Addressed counter issues by debugging and refining the UART logic, and resolved data transmission errors by adjusting the baud rate and
ensuring proper configuration.
• Successfully enhanced the debugging capabilities of the traffic light controller, providing real‑time feedback and improving system reliability.
